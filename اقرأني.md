# منصة باحث (Baheth) — مساعد أكاديمي ذكي للطلاب الجامعيين

منصة SaaS بلغة PHP و MySQL، تدعم OpenAI وAnthropic Claude (قابلة للتبديل من الإعدادات)، جاهزة للعمل مباشرة على استضافة Hostinger (استضافة مشتركة، بدون حاجة لـ SSH أو Composer).

## ما تحتويه المنصة

**حساب الطالب / التدريسي:** (نفس الميزات لكلا النوعين، يُختار نوع الحساب عند التسجيل)
- مساعد أكاديمي ذكي (محادثة) يفهم التخصص والكلية والجامعة والمرحلة الدراسية، مع حفظ سجل المحادثات
- تحليل مستندات PDF وWord (.docx): رفع ملف والحصول على تلخيص وتحليل فوري، مع إمكانية طرح أسئلة متابعة عن نفس المستند
- مولّد خطط بحث: إدخال الموضوع والتخصص وملاحظات متطلبات الجامعة، للحصول على خطة بحث أكاديمية منظمة
- **البحث عن مصادر علمية حقيقية** عبر CrossRef (بيانات فعلية من ملايين الأبحاث المفهرسة، وليست مولّدة بالذكاء الاصطناعي) وحفظها
- **منظم مراجع** مع تنسيق تلقائي بأنماط APA وMLA وChicago
- **مساعد الكتابة الأكاديمية**: توليد مسودة قسم محدد من البحث (مقدمة، إطار نظري، منهجية، مناقشة، خاتمة...)
- **توليد عروض تقديمية احترافية**: يولّد ملف PowerPoint (.pptx) حقيقي وقابل للتحميل والتعديل، من موضوع حر، خطة بحث محفوظة، أو **ملف PDF/Word مرفوع** (يُستخرج محتواه تلقائيًا ليكون مصدر العرض). التصميم يشمل: خلفية متدرجة بألوان الهوية لشريحة العنوان، أيقونة (emoji) مختلفة ومرتبطة فعليًا بمحتوى كل شريحة يختارها الذكاء الاصطناعي تلقائيًا، عناصر زخرفية (دوائر وحلقات ونقاط) لعمق بصري، انتقالات حركية بين الشرائح (Transitions)، وصور فوتوغرافية حقيقية مرتبطة بمحتوى كل شريحة (اختياري، يتطلب مفتاح Pexels مجاني — راجع قسم "صور العروض التقديمية" أدناه)
- **منشئ التقارير والأوراق البحثية الذكي**: يبني تقريرًا أو ورقة بحثية كاملة خطوة بخطوة —
  1. اختيار نوع المستند (ورقة بحثية، تقرير أكاديمي، تقرير تدريب، تقرير مختبر، خطة بحث، مقترح بحث، مراجعة أدبية، دراسة حالة، فصل رسالة، تقرير إداري، وغيرها)
  2. تعبئة بيانات البحث (الموضوع، التخصص، الجامعة، المرحلة، عدد الصفحات المطلوب...)
  3. مخطط أقسام مقترح بالذكاء الاصطناعي أو هيكل افتراضي جاهز، قابل للتعديل والترتيب والتفعيل/التعطيل
  4. توليد محتوى كل قسم منفصلًا بالذكاء الاصطناعي، مع إمكانية توليدها كلها دفعة واحدة
  5. محرر نصوص مباشر (Rich Text) مع حفظ تلقائي، وأدوات ذكاء اصطناعي لتحرير أي نص محدد (إعادة صياغة، أسلوب أكاديمي، اختصار، توسيع، تبسيط، تصحيح لغوي)
  6. رفع ملفات PDF/Word كمصادر يعتمد عليها الذكاء الاصطناعي عند الكتابة
  7. إدارة المراجع يدويًا بتنسيق APA7 تلقائي، مع إمكانية الاستيراد من مكتبة المراجع الشخصية
  8. تصدير المستند كملف Word حقيقي (.docx بغلاف وفهرس ومراجع) أو كصفحة معدّة للطباعة/الحفظ كـ PDF من المتصفح
- **نشر بحث**: بعد إتمام بحث (عبر منشئ التقارير أو خارجه)، يرفعه الطالب كملف PDF لمراجعة الإدارة، وبعد الموافقة يظهر بمكتبة الأبحاث العامة
- تعديل البيانات الأكاديمية وتغيير كلمة المرور من صفحة "حسابي"
- صفحة "الاشتراك": ترقية للخطة المدفوعة عبر تحويل بنكي/دفع يدوي + رفع صورة إثبات الدفع

**صفحة عامة (بدون تسجيل دخول) — "المكتبات":**
- **القرآن الكريم**: تصفّح السور الـ114 بالنص العربي (بخط Amiri Quran)، مع إمكانية عرض ترجمة إنجليزية لكل سورة (بيانات حيّة من QuranEnc API)
- **الكتب الإسلامية**: مكتبة كتب إسلامية (عقيدة، فقه، سيرة، شروح أحاديث وغيرها) مع تحميل مباشر بصيغة PDF (بيانات حيّة من IslamHouse API، أكثر من 5000 كتاب)
- **مكتبة الأبحاث**: تصفّح وبحث وتحميل الأبحاث المنشورة والموافق عليها من الإدارة، مجانًا وبدون حساب
- **بحث فوري (بدون ضغط زر)**:
  - خانة واحدة بصفحة القرآن تبحث آنيًا عن اسم سورة، أو نص آية، أو رقم "سورة:آية" مباشر (مثل 2:255) للانتقال الفوري لها
  - خانة بحث فورية بصفحة الكتب عن العنوان أو المؤلف عبر كامل المكتبة (أكثر من 5000 كتاب)

> ملاحظة تقنية: محتوى القرآن والكتب الإسلامية يُجلب مباشرة من واجهتي IslamHouse وQuranEnc البرمجيتين (مجانيتين، بدون مفتاح API) — ملفات PDF الكتب نفسها تبقى على سيرفرات IslamHouse ولا تُخزَّن على استضافتك إطلاقًا. يُخزَّن محليًا فقط كاش نصي خفيف (JSON) بمجلد `cache/library/` لتسريع الصفحات وتقليل الطلبات الخارجية، بالإضافة لفهرس بحث محلي (كتب + نص آيات) يُبنى تدريجيًا عبر الزيارات الفعلية لأن واجهة IslamHouse لا تدعم بحثًا خادميًا مباشرًا. المجلد يُنشأ تلقائيًا؛ فقط تأكد أن صلاحيات الكتابة متاحة له على الاستضافة (عادة متاحة افتراضيًا).

**حساب المدير:**
- إحصائيات عامة عن المنصة (المستخدمون، المحادثات، المستندات، خطط البحث، طلبات الدفع المعلّقة)
- إدارة المستخدمين: تفعيل/إيقاف الحساب، تصفير عداد الاستخدام الشهري، ترقية/إرجاع الخطة يدويًا
- **اشتراكات الجامعات**: تحديد حد شهري مرتفع لجامعة معيّنة يستفيد منه تلقائيًا كل طالب/تدريسي مسجّل بنفس اسم الجامعة
- مراجعة طلبات الدفع: عرض صورة الإثبات المرفوعة والموافقة عليها أو رفضها
- **إدارة أنواع التقارير**: إضافة/تعديل/تعطيل أنواع مستندات منشئ التقارير وهيكلها الافتراضي
- **إحصائيات منشئ التقارير**: عدد المستندات والأقسام والملفات، عدد طلبات الذكاء الاصطناعي والتوكنات المستهلكة والتكلفة التقديرية بالدولار، تفصيل حسب نوع المستند، وسجل بآخر الطلبات
- **مراجعة الأبحاث المنشورة**: الموافقة على الأبحاث المرسلة أو رفضها قبل ظهورها بمكتبة الأبحاث العامة
- إعدادات: الحدود الشهرية للخطتين، اختيار مزوّد الذكاء الاصطناعي النشط (OpenAI أو Anthropic) واسم الموديل لكل منهما، وبيانات حساب استلام الدفع

**نظام الدفع حاليًا يدوي بالكامل** (بدون بوابة دفع إلكترونية): الأدمن يدخل رقم حسابه/بطاقته من الإعدادات، والطالب يحوّل المبلغ خارج المنصة ثم يرفع صورة إثبات التحويل، والأدمن يوافق يدويًا فترتقي الخطة.

## البنية القابلة للتوسع (مزوّدو الذكاء الاصطناعي)

كل طلبات الذكاء الاصطناعي تمر عبر `includes/ai.php` الذي يوجّهها لمزوّد `includes/ai_providers/openai.php` أو `includes/ai_providers/anthropic.php` حسب اختيار الأدمن من الإعدادات — بدون أي تعديل على بقية صفحات المنصة. لإضافة مزوّد جديد (مثل Google Gemini) مستقبلًا: يكفي إنشاء ملف مشابه بنفس المجلد وربطه بنفس النمط.

كل استدعاء ذكاء اصطناعي ضمن منشئ التقارير يُسجَّل بجدول `ai_requests` (عدد التوكنات والتكلفة التقديرية)، ما يتيح متابعة الاستهلاك من لوحة تحكم المدير.

## خطوات الرفع على Hostinger

### الخطوة 1: إنشاء قاعدة بيانات MySQL
1. ادخل إلى لوحة تحكم Hostinger (hPanel) ← قواعد البيانات ← قواعد بيانات MySQL
2. أنشئ قاعدة بيانات جديدة (مثلًا `baheth_db`)، ومستخدم جديد، وامنحه كل الصلاحيات على القاعدة
3. احتفظ بـ: اسم القاعدة، اسم المستخدم، كلمة المرور، واسم الخادم (غالبًا `localhost`)

### الخطوة 2: الحصول على مفتاح OpenAI API (أو Anthropic)
1. لـ OpenAI: افتح **platform.openai.com** ← API Keys ← أنشئ مفتاحًا جديدًا، وتأكد من إضافة رصيد (billing)
2. لـ Anthropic (اختياري، يمكن تفعيله لاحقًا من الإعدادات): **console.anthropic.com** ← API Keys
> يمكن تخطي هذه الخطوة الآن وإضافة المفتاح لاحقًا من `config.php`، لكن ميزات الذكاء الاصطناعي لن تعمل قبل إضافته وتفعيل الرصيد.

### الخطوة 3: رفع الملفات وتشغيل المعالج
1. ادخل إلى **مدير الملفات (File Manager)** في hPanel، أو استخدم FTP
2. ارفع **كل محتويات** هذا المجلد إلى `public_html` (أو المجلد الفرعي الذي تريد استضافة المنصة عليه)
3. افتح الموقع من المتصفح — سيحوّلك تلقائيًا لصفحة **التثبيت**
4. عبّئ بيانات قاعدة البيانات، وحساب المدير، ومفتاح OpenAI API إن كان جاهزًا
5. اضغط "تثبيت المنصة الآن"
6. بعد الدخول كمدير، افتح **الإعدادات** وأدخل بيانات حساب استلام الدفع (رقم البطاقة/الحساب)

> ملاحظة: لا تحذف ملف `config.php` بعد التثبيت — يحتوي على إعدادات الاتصال بقاعدة البيانات ومفاتيح API.
> ملاحظة تقنية: تحليل ملفات Word وتصدير ملفات Word من منشئ التقارير يحتاجان إضافة `zip` مفعّلة بـ PHP — مفعّلة افتراضيًا على أغلب استضافات Hostinger، لكن يُستحسن التأكد بعد الرفع (لوحة تحكم Hostinger ← PHP Configuration ← تأكد من تفعيل zip).

### إذا كانت المنصة مثبّتة مسبقًا (تحديث لموقع قائم)
لا تعد تشغيل معالج التثبيت. بدلًا من ذلك:
1. ارفع الملفات الجديدة/المعدّلة فقط فوق الملفات الموجودة (لن يُحذف `config.php` لأنه غير موجود بهذه الحزمة أصلًا)
2. افتح **phpMyAdmin** من hPanel، اختر قاعدة بياناتك، وافتح تبويب **SQL**
3. الصق محتوى ملف `baheth-migration.sql` بالكامل واضغط **Go** (يضيف فقط الجداول الناقصة، لا يمسح أي بيانات)
4. إذا كانت هذه أول مرة تحصل فيها على ميزة "منشئ التقارير ونشر الأبحاث"، الصق أيضًا محتوى ملف `baheth-migration-reports.sql` واضغط **Go**

## إضافة/تعديل مفاتيح API لاحقًا

من مدير ملفات Hostinger، افتح `config.php` وعدّل الأسطر:
```php
define('OPENAI_API_KEY', 'مفتاحك هنا...');
define('ANTHROPIC_API_KEY', 'مفتاحك هنا إن رغبت بتفعيله...');
```
ثم اختر المزوّد النشط من لوحة تحكم المدير ← الإعدادات.

## صور العروض التقديمية (Pexels) — اختياري

منشئ العروض التقديمية يضيف صورًا فوتوغرافية حقيقية مرتبطة بمحتوى كل شريحة تلقائيًا، إذا كان مفتاح Pexels معدًّا. بدونه تعمل الميزة طبيعيًا لكن بدون صور.

1. سجّل حسابًا مجانيًا فوريًا (بدون بطاقة دفع) على **pexels.com/api**
2. انسخ الـ API Key من لوحة حسابك
3. افتح `config.php` وأضف سطرًا جديدًا:
```php
define('PEXELS_API_KEY', 'مفتاحك هنا...');
```
4. احفظ الملف — الميزة تعمل تلقائيًا من الطلب التالي بدون أي إعداد إضافي

## الأمان

- كل كلمات المرور مشفّرة (password_hash) ولا تُخزَّن كنص صريح
- جميع الاستعلامات تستخدم Prepared Statements لمنع SQL Injection
- ملف `config.php` محمي بحيث لا يمكن الوصول له مباشرة عبر المتصفح
- تم تفعيل حماية CSRF على كل النماذج
- ملفات المستخدمين المرفوعة (مستندات، صور إثبات الدفع، ملفات العروض التقديمية، ملفات مصادر التقارير، الأبحاث المنشورة) محمية من الوصول المباشر عبر المتصفح — تُعرض فقط عبر صفحات محمية بصلاحيات، باستثناء الأبحاث المنشورة الموافق عليها فقط والمتاحة عمدًا للتحميل العام من مكتبة الأبحاث

## إعادة التثبيت / نسيان بيانات الدخول

- لإعادة التثبيت من جديد: احذف ملف `config.php` من مدير الملفات ثم أعد زيارة الموقع
- لإعادة تعيين كلمة مرور المدير: يتطلب الدخول إلى phpMyAdmin وتحديث حقل `password_hash` في جدول `users` يدويًا

## خارطة الطريق (غير مبني بعد)

- تخصيص المنصة لكل جامعة (قوالب وتعليمات كتابة رسائل خاصة بكل جامعة)
- حسابات جامعات مؤسسية بمستخدم إداري خاص يدير طلابها (حاليًا الدعم الجامعي عبارة عن حد استخدام مرتفع تلقائي فقط، وليس حساب إداري منفصل)
- أنماط استشهاد إضافية لمنشئ التقارير (حاليًا APA7 فقط، الخطة الكاملة تشمل MLA وChicago وHarvard وIEEE وVancouver)
- بوابة دفع إلكترونية حقيقية (Stripe أو بوابة عراقية محلية) بدل المراجعة اليدوية الحالية
- تطبيقات Android وiOS (المنصة حاليًا موقع ويب فقط، بدون طبقة API مخصصة لتطبيقات الجوال)
- نظام "نسيت كلمة المرور" ذاتي عبر البريد الإلكتروني (حاليًا يتطلب تدخل الأدمن يدويًا عبر phpMyAdmin)
- إشعارات بريد إلكتروني (موافقة/رفض الدفع، موافقة/رفض نشر بحث، ترحيب، إلخ) — تحتاج إعداد SMTP
